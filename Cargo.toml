[package]
name = "rust-file"
version = "0.1.0"
edition = "2024"
description = "A GNU file-compatible file type detection tool written in Rust"
license = "MIT"

[[bin]]
name = "file"
path = "src/main.rs"

[dependencies]

[profile.release]
lto = true
strip = true
